document.addEventListener('DOMContentLoaded', () => {
    const doctorContainer = document.getElementById('doctorContainer');
    doctorContainer.style.animation = 'slideInRight 0.5s forwards';
});
